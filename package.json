{
  "name": "react-proj",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install": "yarn --cwd custom-antd-libs install && yarn --cwd react-app install",
    "lib-build": "yarn --cwd custom-antd-libs build-dev",
    "lib-add": "yarn --cwd react-app add ../custom-antd-libs/build-dev",
    "start": "yarn --cwd react-app start",
    "lib-build-add-start": "yarn lib-build && yarn lib-add && yarn start",
    "lib-dist": "yarn --cwd custom-antd-libs build"
  }
}
